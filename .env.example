# Copy to .env.local and fill in once the backend track's /events endpoint is live.
# Leave unset (or "mock") to keep rehearsing against the mock scenarios.

# mock | sse | websocket
VITE_FEED_TRANSPORT=mock

# Only used when VITE_FEED_TRANSPORT is sse or websocket.
VITE_EVENTS_URL=http://localhost:8000/events
